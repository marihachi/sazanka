# エージェント向けドキュメント

このプロジェクトで作業するエージェントに伝えておきたい事柄をまとめている。守るべき規則だけでなく、背景、設計上の意図、既知の制約なども含む。作業前に、関係する関心事のドキュメントを読むこと。

このアプリ固有の用語と、コードでの名前の対応は[用語](docs/agent/glossary.md)にある。

エージェントに指示する開発者は複数人いる想定で書く。開発環境（OS、ブラウザ、エディター）も開発者毎に違うので、特定の環境を前提にしない。

## ドキュメント一覧

- [用語](docs/agent/glossary.md) — このアプリ固有の用語と、コードでの名前の対応
- [作業の進め方](docs/agent/workflow.md) — 言語、検証、コミット、公開
- [ソースの構成とリファクタリング](docs/agent/structure.md) — `src/` のフォルダ分けと依存の向き、コードの置き場所の決め方
- [コードの書き方](docs/agent/coding-style.md) — Biome の設定の理由と、Biome が判断しない書き方の取り決め
- [シミュレーション](docs/agent/simulation.md) — 評価モデルの前提と、壊してはいけない不変条件
- [編集と元に戻す](docs/agent/editing.md) — 元に戻す対象の決め方
- [保存データ](docs/agent/persistence.md) — localStorage の互換性
- [UI](docs/agent/ui.md) — 画面の配置、ボタン、ダイアログ、ヒント
- [アイコンとロゴ](docs/agent/icons.md) — 置き場所、描画方式、色
- [シートの表示](docs/agent/view.md) — シートの大きさ、スクロールと拡大縮小
- [画面操作](docs/agent/interaction.md) — 選択・配線・削除などの操作の割り当て
- [ライセンス](docs/agent/licenses.md) — 同梱しているライブラリと素材のライセンス表示

## このドキュメント自体の運用

- エージェントに伝えておいた方が良い事柄は、作業の中で見つけ次第ここに追記していく。規則に限らず、説明しておくべきことなら何でもよい。
- 実装するたびに、そのとき注意した点や考えたことを「実装のドキュメント」として `docs/agent/` に残す。作業が終わってからまとめて思い出そうとしない。残すのは次のようなもの。
  - なぜその形にしたか（ほかのやり方もあった場合は、選んだ理由）。
  - 気付いた落とし穴と、その避け方。直した不具合も、同じ間違いをしやすいなら書く。
  - 触ると壊れるもの（ほかの場所と合わせて直す必要があるもの、守らないと動かなくなる前提）。
  - 開発者と決めた取り決めと、その理由。
- コードを読めば十分分かる内容は書かない。書くのは、設計上の意図、暗黙の不変条件、プロジェクトの取り決め、ハマりどころなど、コードからは読み取りにくいもの。
- 過去の経緯（以前の名前、検討して採らなかった案、過去に起きた不具合など）は、今後の判断に役立たないなら書かない。
- 追記するだけでなく、定期的に見直す。作業で触った部分については、関係するドキュメントが今のコードと合っているかを毎回確かめ、合わなくなった記述は直し、コードを読めば分かるようになった記述や、役目を終えた記述は消す。
- 関心事ごとに `docs/agent/` 以下の別ファイルに分ける。該当する関心事のファイルがなければ新しく作ってよい。作ったら上の一覧に追加する。
- 実装のドキュメントは、細かく分けていく。ある実装を変えようとする開発者やエージェントが、**そのファイルだけを読めば済む**ようにするため。1つのファイルに別の関心事が混ざってきたら、そこで分ける。分けたら、元のファイルから1行で案内を入れる。
- エージェント向けの入口はこの `AGENTS.md` に一本化する。`CLAUDE.md`（Claude Code 用）は、この `AGENTS.md` を読み込むだけのファイルにしている。内容はそれらに書かず、ここか `docs/agent/` に書くこと。
- 特定のエージェントにだけ当てはまる内容（Claude Code のフックなど）は、各ドキュメントの中でエージェント名の見出し（例: `## Claude Code`）を立てて分ける。共通の内容と混ぜない。
