# エージェント向けドキュメント

このプロジェクトで作業するエージェントが読むべきことを書いている。

プロジェクトの開発者は複数人いる想定で書く。開発環境（OS、ブラウザ、エディター）も開発者毎に違うので、特定の環境を前提にしない。

## ドキュメントの読み方

ドキュメントは2つに分けている。まず「作業前に必ず読むドキュメント」を読み、そのうえで、これから触るものに当てはまる実装のドキュメントを読む。

### 作業前に必ず読むドキュメント

- [用語](docs/agent/glossary.md) — このアプリ固有の言葉と、コードでの名前の対応
- [作業の進め方](docs/agent/workflow.md) — 言語、検証、コミット、公開
- [コードの書き方](docs/agent/coding-style.md) — Biome の設定の理由と、Biome が判断しない書き方の取り決め
- [ソースの構成](docs/agent/structure.md) — `src/` のフォルダ分けと、各ファイルの責務

### 触るときに読むドキュメント（実装のドキュメント）

| 触るもの                               | 読むファイル                                             |
| -------------------------------------- | -------------------------------------------------------- |
| シミュレーション（評価、遅延、tick）   | [シミュレーション](docs/agent/simulation.md)             |
| 編集、元に戻す / やり直し              | [編集と元に戻す](docs/agent/editing.md)                  |
| 保存データ、共有用 JSON                | [保存データ](docs/agent/persistence.md)                  |
| 画面の配置、ボタン、ダイアログ、ヒント | [UI](docs/agent/ui.md)                                   |
| アイコン、ロゴ、色                     | [アイコンとロゴ](docs/agent/icons.md)                    |
| シートのスクロールと拡大縮小           | [シートの表示](docs/agent/view.md)                       |
| 操作の割り当て（選択、配線、削除など） | [画面操作](docs/agent/interaction.md)                    |
| ライブラリや素材の追加                 | [ライセンス](docs/agent/licenses.md)                     |
| ファイルの分け方、構成の変更           | [コードの置き場所の決め方](docs/agent/code-placement.md) |
| Skill の追加・編集                     | [Skill の追加・編集](docs/agent/skills.md)               |

### 指示と食い違ったら

- 開発者の指示が優先。ドキュメントと違うことを指示されたら、指示に従う。
- その指示が今後も続く取り決めなら、その場でドキュメントを直す。1回限りなら直さない。
- ドキュメントにも指示にもないことで迷ったら、決め打ちせずに開発者に聞く。

## ドキュメントの書き方

作業中に気付いたことは、その場でドキュメントに書く。

### 何を書くか

- 規則のほか、背景、設計上の意図、既知の制約など、説明しておくべきことなら何でも書いてよい。
- 実装のたびに、注意した点や考えたことを実装のドキュメントに残す。後でまとめて思い出そうとしない。
  - なぜその形にしたか（ほかの案があったなら、選んだ理由）。
  - 気付いた落とし穴と、その避け方。
  - 触ると壊れるもの（合わせて直す必要があるもの、守らないと動かなくなる前提）。
  - 開発者と決めた取り決めと、その理由。
- コードを読めば分かることは書かない。
- 今後の判断に役立たない過去の経緯（以前の名前、採らなかった案、直した不具合）は書かない。

### どこに書くか

- コードと同じく、責務（[コードの置き場所の決め方](docs/agent/code-placement.md)）ごとに `docs/agent/` の別ファイルに分ける。ある実装を変えようとする人が、そのファイルだけ読めば済むようにするため。
- 1つのファイルに別の責務が混ざってきたら分ける。分けたら、元のファイルから1行で案内する。
- ファイルを作ったら、上のどちらかの一覧に載せる。どの作業でも効くものが「必ず読む」、特定の実装を触るときだけ効くものが「触るとき」。
- 入口はこの `AGENTS.md` だけ。`CLAUDE.md`（Claude Code 用）は、これを読み込むだけにする。
- 特定のエージェントにだけ当てはまる内容（Claude Code のフックなど）は、各ドキュメントの中でエージェント名の小見出し（例: `## Claude Code`）を立てて分ける。

### いつ見直すか

- 作業で触った部分は、毎回ドキュメントと今のコードが合っているか確かめる。合わない記述は直し、コードを読めば分かるようになった記述や、役目を終えた記述は消す。
- 大きく作り変えたあとや、開発者に頼まれたときは、触った部分に限らず全体を見直す。手順は Skill の `doc-review` にある。

## Skill

手順をまとめたものは Skill にしている。ドキュメントが「どうなっているか」を説明するのに対し、Skill は「やるときに何を順に踏むか」を書く。どんな Skill があるかは、その `description` を見る（ここには一覧を持たない）。

- Skill はエージェントごとの仕組みで、共通の置き場所が定まっていない。そのため `.agents/skills/` と `.claude/skills/` の両方に、**同じ内容で**置く（`.agents/` を Codex などが、`.claude/` を Claude Code が読む）。片方だけ直さないこと。
- 手順は Skill にだけ書き、ドキュメントには重ねて書かない。ドキュメント側からは1行で案内する。Skill に対応していないエージェントは考慮しない（開発者の方針）。
- Skill があったほうが良いと気付いたら、開発者に提案する（勝手に作らない）。手順を繰り返し説明している、毎回同じ順で踏んでいる、忘れると手戻りになる確認がある、といったものが目印。
  - 提案には、どんなときに使うか、踏む手順の概要、名前の候補を複数含める。手順がまだどこにも書かれていないなら、その場で書き出して見せる。
- 追加・編集の手順は [Skill の追加・編集](docs/agent/skills.md) にある。
