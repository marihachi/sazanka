# エージェント向け規則

作業前に、関係する関心事の規則ドキュメントを読むこと。

## 規則ドキュメント

- [作業の進め方](docs/agent/workflow.md) — 言語、検証、コミット
- [シミュレーション](docs/agent/simulation.md) — 評価モデルの前提と、壊してはいけない不変条件
- [保存データ](docs/agent/persistence.md) — localStorage の互換性

## この規則ドキュメント自体の運用

- エージェントに伝えておいた方が良い内容は、作業の中で見つけ次第ここに追記していく。
- コードを読めば十分分かる内容は書かない。書くのは、設計上の意図、暗黙の不変条件、ユーザーとの取り決め、ハマりどころなど、コードからは読み取りにくいもの。
- 規則は関心事ごとに `docs/agent/` 以下の別ファイルに分ける。該当する関心事のファイルがなければ新しく作ってよい。作ったら上の一覧に追加する。
- エージェント向けの入口はこの `AGENTS.md` に一本化する。`CLAUDE.md`（Claude Code 用）と `GEMINI.md`（Gemini CLI 用）は、この `AGENTS.md` を読み込むだけのファイルにしている。規則はそれらに書かず、ここか `docs/agent/` に書くこと。
